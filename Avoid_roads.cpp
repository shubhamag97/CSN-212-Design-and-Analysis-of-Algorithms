// we basically follow the dp; dp[i][j]=0, if bad node;
//                             dp[i][j]=dp[i-1][j]+dp[i][j-1], else;
#include <bits/stdc++.h>
using namespace std;

int main() {
	int t; cin>>t;
	while(t--)
	{
	int w,d;string s;cin>>w>>d;
	getline(cin,s);
	getline(cin,s);
	int dp[w][d];
	while(
	}
	return 0;
}
